# Deployment Scripts

Scripts for release management and deployment processes.

## Scripts

- `create_release_tags.ps1` - Creates release tags
- `push_all_branches.ps1` - Pushes all branches to remote
- `push_all_branches.sh` - Shell version of branch pushing